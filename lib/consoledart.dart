// Opdracht 1
bool boolCheck() {
  return false;
}

String varCheck() {
  var name = 'Tin Xian';
  return name;
}

int biggerThenZeroCheck() {
  var price = 2;
  return price;
}

int isZeroCheck() {
  var price = 0;
  return price;
}

// Wij zouden de boolean kunnen gebruiken om te checken of iemand zijn postcode zich bevind in een gebied waar wij bezorgen
// of om te checken of ze betaald hebben of niet

// Ook zouden we het checken of de var wel een waarde heeft kunnen gebruiken voor als iemand een review wilt achterlaten zonder
// 1 van de velden niet in te voeren.

// Opdracht 2
int biggerThenZeroCheck2() {
  var price = 2;
  return price;
}

int isZeroCheck2() {
  var price = 0;
  return price;
}

int rangeCheck() {
  var postcode = 17;
  return postcode;
}

// Opdracht 3
List listCheck() {
  var lst = List(3);
  lst[0] = 17;
  lst[1] = 'Second price';
  lst[2] = 16;
  return lst;
}

List splitCheck() {
  var worded = 'Am-I-splitted?';

  var splitted = worded.split('-');

  return splitted;
}

// Een list kunnen we mogelijk gebruiken om de gekozen producten in een array te zetten.
